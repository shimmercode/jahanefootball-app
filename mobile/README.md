# Jahan Football Flutter app

```bash
flutter pub get
flutter analyze
flutter test
flutter run --dart-define=ENV=development
```

Production APK:

```bash
flutter build apk --release --dart-define=ENV=production
```
